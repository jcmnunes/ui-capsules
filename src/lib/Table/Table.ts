import { styled } from '../stitches.config';

export const Table = styled('table', {
  width: '100%',
  borderCollapse: 'collapse',
  borderRadius: '12px',
  borderStyle: 'hidden',
  boxShadow: `0 0 0 1px $colors$neutral300`,
  overflow: 'hidden',
});
